include: package:lints/recommended.yaml
analyzer:
  errors:
    # Vendored protocol code; keep signal high on real errors only.
    avoid_print: ignore
    constant_identifier_names: ignore
linter:
  rules:
    avoid_print: false
